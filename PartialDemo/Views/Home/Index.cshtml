
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>

    @*@for (int i = 0; i < 10; i++)
        {
            <div>
                @Guid.NewGuid().ToString()
            </div>

        }*@


    @*@Html.Action("liste")
        @Html.Action("liste")
        @Html.Action("liste")*@

    @Html.Action("liste")
    @Html.Action("liste2")
    @Html.Action("liste2",new { antal=5}) 

</body>
        </html>
